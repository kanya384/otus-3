apiVersion: v1                                      # API version
kind: Service                                       # Type of kubernetes resource 
metadata:
  name: users-postgres                          # Name of the resource
  labels:                                           # Labels that will be applied to the resource
    app: users-postgres
spec:
  type: NodePort
  ports:
    - port: 5432
  selector:                                         # Selects any Pod with labels `app=users-postgres,tier=postgres`
    app: users-postgres
    tier: postgres